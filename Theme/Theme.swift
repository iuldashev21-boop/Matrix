import SwiftUI

enum Theme {
    // Quick access to common values
    static let backgroundColor = Color.matrixBlack
    static let cardBackground = Color.darkGray
    static let primaryAccent = Color.matrixGreen
    static let primaryText = Color.white
    static let secondaryText = Color.lightGray

    // Component values
    static let cornerRadius: CGFloat = 12
    static let cardCornerRadius: CGFloat = 16
    static let buttonHeight: CGFloat = 50
    static let progressBarHeight: CGFloat = 8

    // Habit formation constant (days to form a habit)
    static let habitFormationDays: Int = 66
}

